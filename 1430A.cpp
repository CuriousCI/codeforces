#include <iostream>
#include <cstdint>
#include <algorithm>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(NULL);
	cin.tie(NULL);

	int16_t cases, windows;
	cin >> cases;

	while (cases--)
	{
		cin >> windows;
		int16_t three = 0, five = 0, seven = 0;
		// auto modulo = windows % 7;
		while (windows >= 3)
		{
			if (windows % 7)
		}
	}
}