#include <iostream>
#include <ranges>
#include <vector>
#include <cstdint>
#include <algorithm>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(NULL);
	cin.tie(NULL);

	int64_t slimes;
	cin >> slimes;

	vector<int64_t> result(1, 1);
	for (auto _ = 1; _ < slimes; _++)
	{
		if (result.back() ==
	}
}