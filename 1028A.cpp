#include <iostream>
#include <vector>
#include <cstdint>
#include <algorithm>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(NULL);
	cin.tie(NULL);

	uint16_t width, height;
	cin >> width >> height;

	vector<string> table(height);
	for (auto &row : table)
		cin >> row;

	for ()
}