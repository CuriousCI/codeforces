#include <iostream>
#include <cstdint>
#include <algorithm>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(NULL);
	cin.tie(NULL);

	int16_t alpha_x, alpha_y, beta_x, beta_y;
	cin >> alpha_x >> alpha_y >> beta_x >> beta_y;
}