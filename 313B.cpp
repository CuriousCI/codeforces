#include <iostream>
#include <map>
#include <cstdint>
#include <algorithm>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(NULL);
	cin.tie(NULL);

	string line;
	cin >> line;
	uint64_t queries_count;
	cin >> queries_count;
	map<pair<uint64_t, uint64_t>> calculated while (queries_count--)
	{
		uint64_t left, right;
		cin >> left >> right;
	}
}