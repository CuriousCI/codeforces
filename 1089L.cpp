#include <iostream>
#include <vector>
#include <cstdint>
#include <algorithm>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(NULL);
	cin.tie(NULL);

	uint32_t idlers_number, jobs_number;
	cin >> idlers_number >> jobs_number;

	vector<uint32_t> jobs(jobs_number)
}