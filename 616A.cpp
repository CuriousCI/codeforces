#include <iostream>
#include <cstdint>
#include <algorithm>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(NULL);
	cin.tie(NULL);

	string alpha, beta;
	cin >> alpha >> beta;

	auto index = 0;
	if (alpha.length() == beta.length())
	{
		bool equal = true;
		for (auto index = 0; index < alpha.length(); index++)
	}
}